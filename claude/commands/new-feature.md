Create the directory claude-features/$ARGUMENTS/ if it doesn't already exist.

If it already exists, inform me and do NOT overwrite anything.

If it doesn't exist, create it and confirm it's ready.

After creating the directory, tell me to run /brainstorm $ARGUMENTS when ready.
